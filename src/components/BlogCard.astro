---
const { title, url, description, date } = Astro.props
---

<li class="bg-mantle bg-cover bg-center bg-no-repeat rounded-2xl p-6 m-4 relative transition-all duration-300 hover:cursor-pointer hover:scale-102">
  <a href={url}>
  <div class="relative z-10">
    <h2 class="text-2xl font-bold mb-2 text-text">{title}</h2>
    <p class="mb-4 text-text">{description}</p>
    <p class="absolute top-0 right-0 text-subtext0">{new Date(date).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })}</p>
    <a href={url} class="bg-surface1 text-text px-4 py-2 rounded-lg hover:bg-surface2 transition-all duration-200 ease-in-out group">
      Read about it 
      <span class="inline-block group-hover:translate-x-1 transform transition-all duration-200 ease-in-out">→</span>
    </a>
  </div>
  </a>
</li>